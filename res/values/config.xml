<resources>
    <integer name="player_pref_subtitle_size_default">12</integer> <!-- an integer instead of a dimension because we don't want it to be density-scaled -->
    <integer name="player_pref_subtitle_vpos_default">0</integer> <!-- an integer instead of a dimension because we don't want it to be density-scaled -->
    <integer name="video_info_pane_width_ratio">55</integer> <!-- the percentage of the screen width used by the Video Info Panel -->
</resources>
